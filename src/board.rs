use std::os::raw::c_void;

struct Board {

}

impl Board {
    // Initializes the board and adds all the pieces to their proper spot
    fn init_board(&self) -> Board {
        // need to implement this
    }

    // Prints out the board to the console
    fn print(&self) -> c_void {
        // need to implement this
    }
}